@model IEnumerable<EscolaColetorWeb.Entity.turma>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dc_turma)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dc_turma)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cd_turma }) |
            @Html.ActionLink("Details", "Details", new { id=item.cd_turma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cd_turma })
        </td>
    </tr>
}

</table>
