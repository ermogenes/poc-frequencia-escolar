@model IEnumerable<EscolaColetorWeb.Entity.aluno>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nm_aluno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.responsavel.nm_responsavel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.turma.dc_turma)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nm_aluno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.responsavel.nm_responsavel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.turma.dc_turma)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cd_aluno }) |
            @Html.ActionLink("Details", "Details", new { id=item.cd_aluno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cd_aluno })
        </td>
    </tr>
}

</table>
